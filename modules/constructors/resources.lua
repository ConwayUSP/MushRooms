require("modules.entities.resource")

---@return Resource
-- cria um recurso do tipo Madeira
function newCaskin()
	local description = "TODO"
	local wood = Resource.new(MATERIAL, CASKIN.name, description)

	return wood
end

---@return Resource
-- cria um recurso do tipo Pedra
function newPedacito()
	local description = "TODO"
	local stone = Resource.new(MATERIAL, PEDACITO.name, description)

	return stone
end

---@return Resource
-- cria um recurso do tipo Ferro
function newPorro()
	local description = "TODO"
	local iron = Resource.new(MATERIAL, PORRO.name, description)

	return iron
end

---@return Resource
-- cria um recurso do tipo Ouro
function newWaw()
	local description = "TODO"
	local gold = Resource.new(MATERIAL, WAW.name, description)

	return gold
end

---@return Resource
-- cria um recurso do tipo Carvão
function newFafogo()
	local description = "TODO"
	local coal = Resource.new(MATERIAL, FAFOGO.name, description)

	return coal
end

---@return Resource
-- cria um recurso do tipo Pena
function newPuff()
	local description = "TODO"
	local feather = Resource.new(MATERIAL, PUFF.name, description)

	return feather
end

---@return Resource
-- cria um recurso do tipo Corda
function newCoseca()
	local description = "TODO"
	local rope = Resource.new(MATERIAL, COSECA.name, description)

	return rope
end

---@return Resource
-- cria um recurso do tipo Vidro
function newArduro()
	local description = "TODO"
	local glass = Resource.new(MATERIAL, ARDURO.name, description)

	return glass
end

---@return Resource
-- cria um recurso do tipo Cogumélium
function newCogumelium()
	local description = "TODO"
	local cogumelium = Resource.new(MATERIAL, COGUMELIUM.name, description)

	return cogumelium
end

---@return Resource
-- cria um recurso do tipo Slime
function newBlub()
	local description = "TODO"
	local slime = Resource.new(MATERIAL, BLUB.name, description)

	return slime
end

---@return Resource
-- cria um recurso do tipo Argila
function newMolle()
	local description = "TODO"
	local clay = Resource.new(MATERIAL, MOLLE.name, description)

	return clay
end

---@return Resource
-- cria um recurso do tipo Óleo
function newEww()
	local description = "TODO"
	local oil = Resource.new(MATERIAL, EWW.name, description)

	return oil
end

---@return Resource
-- cria um recurso do tipo Linha
function newCobrita()
	local description = "TODO"
	local line = Resource.new(MATERIAL, COBRITA.name, description)

	return line
end

---@return Resource
-- cria um recurso do tipo Alface
function newFalho()
	local description = "TODO"
	local lettuce = Resource.new(MATERIAL, FALHO.name, description)

	return lettuce
end

---@return Resource
-- cria um recurso do tipo Grãos
function newTumbu()
	local description = "TODO"
	local beans = Resource.new(MATERIAL, TUMBU.name, description)

	return beans
end

---@return Resource
-- cria um recurso do tipo Alho/Cebola
function newFroncho()
	local description = "TODO"
	local garlic = Resource.new(MATERIAL, FRONCHO.name, description)

	return garlic
end

---@return Resource
-- cria um recurso do tipo Ervas/Tempero
function newFunpo()
	local description = "TODO"
	local herbs = Resource.new(MATERIAL, FUNPO.name, description)

	return herbs
end

---@return Resource
-- cria um recurso do tipo Pimenta
function newGraah()
	local description = "TODO"
	local pepper = Resource.new(MATERIAL, GRAAH.name, description)

	return pepper
end

---@return Resource
-- cria um recurso do tipo Cenoura
function newCabra()
	local description = "TODO"
	local carrot = Resource.new(MATERIAL, CABRA.name, description)

	return carrot
end

---@return Resource
-- cria um recurso do tipo Batata
function newChubo()
	local description = "TODO"
	local potato = Resource.new(MATERIAL, CHUBO.name, description)

	return potato
end

---@return Resource
-- cria um recurso do tipo Trigo
function newJifofa()
	local description = "TODO"
	local wheat = Resource.new(MATERIAL, JIFOFA.name, description)

	return wheat
end

---@return Resource
-- cria um recurso do tipo Carne
function newBiff()
	local description = "TODO"
	local meat = Resource.new(MATERIAL, BIFF.name, description)

	return meat
end

---@return Resource
-- cria um recurso do tipo Leite
function newYuli()
	local description = "TODO"
	local milk = Resource.new(MATERIAL, YULI.name, description)

	return milk
end

---@return Resource
-- cria um recurso do tipo Açúcar
function newNham()
	local description = "TODO"
	local sugar = Resource.new(MATERIAL, NHAM.name, description)

	return sugar
end

---@return Resource
-- cria um recurso do tipo Ovo
function newPlop()
	local description = "TODO"
	local egg = Resource.new(MATERIAL, PLOP.name, description)

	return egg
end

---@return Resource
-- cria um recurso do tipo Peixe
function newMelsh()
	local description = "TODO"
	local fish = Resource.new(MATERIAL, MELSH.name, description)

	return fish
end

---@return Resource
-- cria um recurso do tipo Cogumelo
function newChongo()
	local description = "TODO"
	local mushroom = Resource.new(MATERIAL, CHONGO.name, description)

	return mushroom
end

---@return Resource
-- cria um recurso do tipo Queijo
function newWamoli()
	local description = "TODO"
	local cheese = Resource.new(MATERIAL, WAMOLI.name, description)

	return cheese
end

---@return Resource
-- cria um recurso do tipo Mel
function newZub()
	local description = "TODO"
	local honey = Resource.new(MATERIAL, ZUB.name, description)

	return honey
end
